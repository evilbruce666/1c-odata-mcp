# 1c-odata-mcp

MCP-сервер для доступа к данным **1С:Предприятие** через стандартный интерфейс **OData** — только на чтение. Превращает технический OData в набор понятных инструментов для **руководителя** и **бухгалтера**: контрагенты, документы, остатки, дебиторка, продажи, движение денег.

Работает локально (или на вашем сервере), подключается к **Claude Desktop** и **Claude Code**. Никаких компонентов на стороне 1С ставить не нужно — достаточно опубликованного OData.

> **Статус:** 🚧 в активной разработке. Ниже отмечено, что уже готово, а что в работе. README обновляется по мере реализации.

> 📘 **Подключение оказалось с нюансами** (облачная учётка вместо отдельного юзера, пустой «Состав OData», `+` vs `%20`). Полное пошаговое руководство с чек-листами настроек и таблицей диагностики — в **[docs/CONNECTING.md](docs/CONNECTING.md)**. Ниже — краткая версия.

---

## Зачем это нужно

Сырой OData 1С — это сотни технических `EntitySet` с кириллическими именами (`Catalog_Контрагенты`, `Document_РеализацияТоваровУслуг`, `AccumulationRegister_ТоварыНаСкладах`) и GUID-ключами. Работать с ним из чата невозможно.

Этот сервер прячет всю технику за высокоуровневыми инструментами. Вы спрашиваете обычным языком:

- «Покажи дебиторку» → сальдо счёта 62 из регистра бухгалтерии
- «История по контрагенту Ромашка» → все документы + взаиморасчёты
- «Остатки на складе» → виртуальная таблица Balance регистра накопления
- «Продажи за май» → обороты по реализациям

---

## Безопасность: только чтение

На первом этапе **любые операции записи запрещены**. Защита в три контура:

1. **HTTP-клиент** делает только `GET`; гард `assertReadOnly()` блокирует прочие методы.
2. **Конфигурация** `READ_ONLY=true` (менять на 1-м этапе нельзя).
3. **На стороне 1С** в «Состав OData» включаются только нужные объекты — даже теоретически сервер не видит лишнего.

Секреты — только из `.env`, в логи не попадают (заголовок `Authorization` редактируется). Пароль в репозиторий не коммитится.

---

## Требования

- **Node.js ≥ 20**
- Любая **1С:Предприятие** с опубликованным стандартным интерфейсом **OData** (своя публикация на IIS/Apache, хостинг 1cfresh, Scloud и т.п.)
- Учётные данные с правами на чтение

---

## Установка

```bash
git clone https://github.com/evilbruce666/1c-odata-mcp.git
cd 1c-odata-mcp
npm install
npm run build
```

---

## Настройка OData в 1С

Работает с любой 1С, где опубликован стандартный интерфейс OData. Шаги:

1. Войдите в базу в режиме **«Предприятие»**.
2. **Все функции → Обработки → «Настройка стандартного интерфейса OData»**.
3. На вкладке **«Состав»** отметьте объекты для публикации (справочники Контрагенты, Номенклатура, Организации; нужные документы; регистры). На 1-м этапе достаточно объектов на чтение.
4. Сохраните.

Адрес OData имеет общий вид (точный — зависит от площадки публикации):

```
https://<сервер>/<имя_базы>/odata/standard.odata/
```

Авторизация — **Basic Auth**. ⚠️ Если приходит **401**: на ряде облачных площадок публикация ждёт учётку **аккаунта площадки**, а не пользователя внутри базы; для пользователя 1С нужна «Аутентификация 1С:Предприятия» с паролем. Разбор по площадкам (в т.ч. Scloud) и таблица диагностики — в **[docs/CONNECTING.md](docs/CONNECTING.md)**.

Проверка вручную (вернётся список объектов в JSON):

```bash
curl -u "ПОЛЬЗОВАТЕЛЬ:ПАРОЛЬ" \
  "https://<сервер>/<имя_базы>/odata/standard.odata/?$format=json"
```

---

## Конфигурация (`.env`)

Скопируйте `.env.example` в `.env` и заполните:

```bash
cp .env.example .env
```

| Переменная | Назначение | По умолчанию |
|---|---|---|
| `ODATA_BASE_URL` | Базовый URL OData (со слэшем на конце) | — |
| `ODATA_USERNAME` / `ODATA_PASSWORD` | Basic Auth | — |
| `ODATA_TIMEOUT_MS` | Таймаут запроса, мс | `30000` |
| `ODATA_RETRIES` | Повторы при 429/5xx | `3` |
| `ODATA_PAGE_SIZE` | Размер страницы (`$top`) | `100` |
| `ODATA_MAX_ROWS` | Защитный максимум строк за вызов | `1000` |
| `LOG_LEVEL` | Уровень логов (в stderr) | `info` |
| `READ_ONLY` | Жёсткий режим чтения | `true` |

---

## Запуск

```bash
npm start
```

Сервер общается по протоколу MCP через **stdio**. Обычно его не запускают руками — его поднимает клиент (Claude Desktop / Claude Code). Команда выше полезна для проверки, что всё стартует без ошибок (логи — в stderr).

---

## Подключение к Claude Desktop

Откройте конфиг:

- **macOS:** `~/Library/Application Support/Claude/claude_desktop_config.json`
- **Windows:** `%APPDATA%\Claude\claude_desktop_config.json`

Добавьте сервер в `mcpServers` (не удаляя существующие). Пароль в конфиг **не** вписываем — он берётся из `.env` через `--env-file`:

```json
{
  "mcpServers": {
    "1c-odata": {
      "command": "/opt/homebrew/opt/node@22/bin/node",
      "args": [
        "--env-file=/Users/<вы>/Projects/1c-odata-mcp/.env",
        "/Users/<вы>/Projects/1c-odata-mcp/dist/index.js"
      ]
    }
  }
}
```

**Полностью перезапустите Claude Desktop** (Cmd+Q, не просто окно). В списке инструментов появится `1c-odata`.

Частые грабли (полностью разобраны в [docs/CONNECTING.md](docs/CONNECTING.md)):
- укажите **абсолютный путь к `node`** (GUI может не видеть PATH; `which node`);
- **абсолютные** пути к `.env` и `dist/index.js`;
- пароль не вписывайте в конфиг — он берётся из `.env` через `--env-file` (нужен Node ≥ 20.6).

---

## Подключение к Claude Code

```bash
claude mcp add 1c-odata -s user -- /opt/homebrew/opt/node@22/bin/node \
  --env-file=/Users/<вы>/Projects/1c-odata-mcp/.env \
  /Users/<вы>/Projects/1c-odata-mcp/dist/index.js
```

Проверка: `claude mcp list`. Подробности и диагностика — [docs/CONNECTING.md](docs/CONNECTING.md).

---

## Инструменты

У всех инструментов есть необязательный параметр **`database`** (какая база 1С — см. `list_databases`); у аналитических — ещё и **`organization`** (фильтр по юрлицу — см. `list_organizations`).

| Инструмент | Что делает | Статус |
|---|---|---|
| `list_databases` | Список настроенных баз 1С (для параметра `database`) | ✅ |
| `list_organizations` | Организации (юрлица) внутри базы (для параметра `organization`) | ✅ |
| `list_entities` | Карта объектов базы по классам (справочники/документы/регистры) | ✅ |
| `describe_entity` | Поля и связи конкретного объекта (из `$metadata`) | ✅ |
| `find_counterparty` | Поиск контрагента по названию или ИНН | ✅ |
| `get_counterparty` | Карточка контрагента + договоры/счета | ✅ |
| `search_documents` | Поиск документов по типу/дате/контрагенту/сумме | ✅ |
| `get_document` | Документ с табличной частью | ✅ |
| `get_inventory` | Остатки товаров (сч. 41/10/43) | ✅ |
| `get_debtors` | Дебиторская задолженность (сальдо сч. 62) | ✅ |
| `get_sales` | Продажи/выручка за период | ✅ |
| `get_cashflow` | Движение денежных средств (банк + касса) | ✅ |
| `get_customer_history` | История взаиморасчётов с покупателем | ✅ |
| `get_supplier_history` | История взаиморасчётов с поставщиком | ✅ |
| `health_check` | Проверка соединения и авторизации | ✅ |

Все инструменты проверены на живой базе 1С:Бухгалтерия 3.0.

### Как считается аналитика в БП 3.0

В 1С:Бухгалтерия 3.0 нет отдельного регистра остатков товаров (как в УТ) — учёт ведётся на счетах. Поэтому `get_debtors` и `get_inventory` берут сальдо из виртуальной таблицы `AccountingRegister_Хозрасчетный/Balance`, фильтруя по счёту:
- **Дебиторка** — счёт 62, дебетовое сальдо по контрагентам (`ExtDimension1`); кредит (авансы) вычитается.
- **Остатки** — счета 41/10/43, количество и сумма по номенклатуре (`ExtDimension1`).

Счета резолвятся из `ChartOfAccounts_Хозрасчетный` по префиксу кода, имена контрагентов/номенклатуры — из справочников.

---

## Несколько баз и несколько организаций

Это два разных случая — и решаются они по-разному:

### Несколько отдельных баз (разные OData-адреса)

Один сервер обслуживает несколько баз. В `.env` опишите каждую тройкой переменных
(полный пример — в `.env.example`):

```bash
ODATA_DB_BUH_BASE_URL=https://<сервер>/<база1>/odata/standard.odata/
ODATA_DB_BUH_USERNAME=login_1
ODATA_DB_BUH_PASSWORD=secret1
ODATA_DB_BUH_LABEL=Бухгалтерия ООО «Ромашка»

ODATA_DB_TORG_BASE_URL=https://<сервер>/<база2>/odata/standard.odata/
ODATA_DB_TORG_USERNAME=login_2
ODATA_DB_TORG_PASSWORD=secret2

ODATA_DEFAULT_DB=buh
```

`<ИМЯ>` (BUH, TORG) — это и есть значение параметра `database`. Узнать список —
инструментом `list_databases`. У каждой базы свой кеш `$metadata`. Старый формат с
одной базой (`ODATA_BASE_URL`) продолжает работать как база `default`.

Примеры запросов: «дебиторка в базе torg», «сравни выручку buh и torg за май».

### Несколько организаций (юрлиц) в одной базе

Бухгалтерия 3.0 ведёт несколько юрлиц в одной базе — отдельное подключение тут не
нужно. У аналитических инструментов есть параметр `organization`: укажите название
юрлица, и данные отфильтруются по нему (`Организация_Key`). Список — `list_organizations`.

Примеры: «остатки по организации Ромашка», «движение денег ИП Иванов за квартал».

---

## Архитектура

```
src/
  index.ts            # точка входа
  context.ts          # реестр баз: Connection (клиент + кеш $metadata) + ServerContext
  mcp/
    server.ts         # инициализация MCP SDK, регистрация инструментов, stdio
  odata/
    client.ts         # fetch-обёртка: Basic-auth, таймауты, retry, логирование
    query.ts          # типобезопасный билдер $filter/$select/$top/$skip/$expand
    pagination.ts     # автопрокрутка страниц до лимита
    metadata.ts       # загрузка и парсинг $metadata (EDMX → карта сущностей)
    accounting.ts     # план счетов + сальдо регистра Хозрасчетный (дебиторка/остатки)
    orgs.ts           # список и резолв организаций (юрлиц)
    errors.ts         # классификация и нормализация ошибок
  tools/
    meta.ts           # list_databases, list_organizations, list_entities, describe_entity, health_check
    counterparties.ts # find/get_counterparty, customer/supplier_history
    documents.ts      # search_documents, get_document
    registers.ts      # get_inventory, get_debtors, get_sales, get_cashflow
  types/
    odata.ts          # типы EDMX и ответов OData
    domain.ts         # доменные типы (Counterparty, Document, Balance…)
  config/
    env.ts            # конфигурация: одна или несколько баз + поведение (zod)
    mapping.ts        # маппинг «человеческое имя ↔ объект 1С / счёт»
  scripts/            # read-only probe-скрипты (metadata / analytics / mcp)
```

**Стек:** Node.js 20+, TypeScript (strict), официальный `@modelcontextprotocol/sdk`, нативный `fetch`, `zod` (валидация), `pino` (логи в stderr), `fast-xml-parser` (разбор EDMX).

**Целевая конфигурация:** 1С:Бухгалтерия предприятия 3.0. Имена объектов определяются автоматически из `$metadata` при первом запуске; маппинг подстраивается под фактическую базу.

---

## Дорожная карта

- [x] Исследование OData, карта сущностей, набор инструментов, архитектура
- [x] OData-клиент (auth, retry, таймауты, пагинация, логирование)
- [x] Разбор `$metadata` и карта сущностей
- [x] Инструменты: meta, counterparties, documents, sales/cashflow
- [x] Публикация на GitHub
- [x] Калибровка `get_inventory` / `get_debtors` на живой базе (регистр Хозрасчетный)
- [x] Прогон на реальном `$metadata` → сверка имён объектов БП 3.0 (1479 объектов, все совпали)
- [x] Несколько баз (`database`) и несколько организаций (`organization`) — проверено e2e
- [ ] (позже) npm-пакет с запуском через `npx`
- [ ] (позже, отдельный этап) операции записи — пока запрещены

## Заметки по реализации

- **`+` vs `%20` в OData 1С.** 1С не декодирует `+` в пробел внутри `$filter` (отвечает 400),
  поэтому query-string собирается через `encodeURIComponent` (пробел → `%20`), а не `URLSearchParams`.
- **Разведка живой базы.** `node --env-file=.env dist/scripts/probe-metadata.js` — карта объектов
  и сверка имён; `…/probe-analytics.js` — проверка дебиторки/остатков. Секреты не печатаются.

---

## Лицензия

MIT
